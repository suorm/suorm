# AGENTS.md

## Project Structure
- Minimal documentation found - likely uses code comments or inline docs
- Check `git status` for untracked documentation files
- Core architecture likely in `src/` or `lib/`

## Workflows
- Run `npm test` for standard test execution
- Use `npm run lint` for code formatting
- Check `package.json` for package boundaries

## Framework Quirks
- No detected config files - assume default conventions
- Look for framework-specific comments in source files

## CI/CD
- No explicit CI config found
- Use `npm run build` for production artifacts
- Check `.gitignore` for deployment exclusions

## Notes
- This guide assumes standard npm/project structure
- Add specific commands if your workflow differs